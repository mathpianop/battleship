const Game = require("../factories/Game");

describe("Game", () => {
  describe("takeHumanTurn", () => {
    xit("updates the DOM after a non-decisive move", () => {

    })

    xit("updates the DOM after a decisive move", () => {
      
    })
  })
})